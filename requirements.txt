setuptools>=68.0.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
# huggingface_hub for listing + downloading parquet files directly
# (replaces datasets library which requires pyarrow which fails to build on Python 3.14)
huggingface_hub>=0.20.0
requests>=2.31.0
# pyarrow bundled with pandas is sufficient for read_parquet — no separate install needed
scikit-learn>=1.3.0
scipy>=1.11.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
rich>=13.0.0
streamlit>=1.32.0
plotly>=5.15.0
matplotlib>=3.7.0
